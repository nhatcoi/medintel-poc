interface:
  display_name: "Medintel Poc"
  short_description: "Repo-specific patterns and workflows for medintel-poc"
  default_prompt: "Use the medintel-poc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true