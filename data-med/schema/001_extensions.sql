-- PostgreSQL extensions (chạy với quyền superuser / RDS: bật từ console)
-- Bỏ qua dòng vector nếu chưa cài pgvector.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- CREATE EXTENSION IF NOT EXISTS vector;
